# BUG-010 获取当前计分规则时伪造空规则

创建一个尚未配置活跃计分规则的赛季，然后调用获取当前规则接口。缺陷态会返回 HTTP 200，并构造一个 ID 和版本号均为 0、却被标记为活跃的空规则；数据库查询取消或其他查询失败也会被包装成相同的成功结果。

正确结果是：未配置活跃规则时返回明确的 404 业务错误且不包含伪造规则数据；数据库查询失败必须保留失败语义；已配置的正常活跃规则仍能完整读取，所有读取操作不得改变数据库中的规则记录。

复现命令：

```text
go test ./scripts/verify -count=1 -run '^TestBug010_BusinessRegression$'
```
