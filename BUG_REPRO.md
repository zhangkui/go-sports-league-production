# BUG-001 赛程比赛懒物化并发返回非持久对象

## 业务入口

`GET /api/v1/schedules/{id}/match`

## 触发条件

准备一条已经存在但还没有 match 记录的赛程，同时发起两个读取比赛详情的请求。

## 实际结果

两个调用可能都返回成功，但返回的比赛 ID 不一致，其中一个对象没有真实数据库记录；之后对该对象写入事件或确认会出现关联异常。

## 正确结果

同一条赛程只能物化出一个比赛实体。所有并发调用都应返回同一个已经写入数据库的比赛对象。

## 验证命令

在 `backend` 目录执行：

```text
go test -race ./scripts/verify -count=10 -run '^TestBug001_BusinessRegression$'
```

该命令启用 race detector 并连续执行 10 次。缺陷状态应稳定命中未持久化对象或并发返回 ID 不一致的业务断言；修复状态应全部通过。