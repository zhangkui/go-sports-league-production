# BUG-002 赛季创建后规则版本指针与活跃规则脱节`n`n创建一个新的足球赛季，随后读取赛季当前版本和活跃计分规则。缺陷态会看到返回对象声称版本为 1，但数据库仍为 0，活跃规则读取为零值对象。`n`n正确结果是赛季版本、活跃规则版本和规则快照全部为 1，且只有一个活跃规则。`n`n复现命令：`n`ngo test ./scripts/verify -count=1 -run '^TestBug002_BusinessRegression$'`n
