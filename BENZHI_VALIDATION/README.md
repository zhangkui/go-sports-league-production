# BUG-010 Validation Evidence

- `pre_fix.txt` records the audited defect-state result from the original Pre trajectory.
- `post_fix.txt` records the audited fixed-state result from the original Post-Pre trajectory.
- `environment.txt` records the local toolchain and Docker target.
- `docker-build.txt` records the Docker image verification result.

Raw JSONL trajectories are delivered separately and are not committed to this branch.
